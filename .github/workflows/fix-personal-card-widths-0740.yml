name: Fix personal system card widths 0.7.40
on:
  push:
    paths:
      - '.github/workflows/fix-personal-card-widths-0740.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply personal card width fix
        run: python .github/scripts/fix-personal-card-widths-0740.py
      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add he/workspace/index.html
          if git diff --cached --quiet; then
            echo 'No changes'
          else
            git commit -m 'Fix personal system card widths; bump to 0.7.40'
            git push origin main
          fi

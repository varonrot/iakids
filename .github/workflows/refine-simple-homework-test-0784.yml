name: Refine simple homework test 0.7.84

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/refine-simple-homework-test-0784.py'
      - '.github/workflows/refine-simple-homework-test-0784.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Refine simple homework test
        run: python .github/scripts/refine-simple-homework-test-0784.py
      - name: Compile backend
        run: python -m py_compile backend-ai-tutor-he/main.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend-ai-tutor-he/main.py he/workspace/index.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Refine simple homework test flow; bump to 0.7.84"
          git push origin main

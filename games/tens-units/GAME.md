# 🔟 עשרות ויחידות

| | |
|---|---|
| **מס׳** | 9/100 |
| **Slug** | `tens-units` |
| **קטגוריה** | 🔢 חשבון ומתמטיקה |
| **מכניקה** | בחירה מרובה (4 אפשרויות) |
| **DB** | `iakids_game_tens-units` (IndexedDB, דרך ה-SDK) |

## תיאור

הצגת מספר (45) ופירוקו ל-4 עשרות ו-5 יחידות.

## לוגיקת תוכן

הגרל מספר; שאל כמה עשרות/יחידות (או מאות בקשה); 4 אפשרויות.

## רמות

קל: עד 99 · בינוני: עד 999 (מאות) · קשה: כתיבה מפורקת 400+50+6

בחירת רמה במסך הפתיחה; מצב 🎲 אקראי מערבב רמות בין שאלות. ברירת מחדל: הרמה האחרונה (נשמרת ב-progress).

## 🪙 מטבעות

- תשובה נכונה: **+10 🪙** (`IAKidsCoins.right(btnEl)` — כולל אנימציית מטבע עף).
- 3 נכונות ברצף: **+5 🪙 בונוס** (אוטומטי ב-SDK).
- טעות: **−5 🪙** (`IAKidsCoins.wrong()`, לא יורד מתחת ל-0).
- סיום משחק: **+25 🪙** (אוטומטי ב-`game.complete()`).

## משימות בנייה

- [ ] העתק שלד מ-`games/demo/index.html` (RTL, game-style.css, game-sdk.js, שלושת המסכים)
- [ ] `IAKidsCoins.mount()` בטעינה — תג המטבעות מוצג
- [ ] עזרה ❓: `IAKidsHelp.mount({slug, how, example})` — הסבר קצר איך משחקים + דוגמה קונקרטית; נפתח אוטומטית בביקור ראשון
- [ ] מסך פתיחה עם בחירת רמה: קל / בינוני / קשה / 🎲 אקראי (ברירת מחדל: הרמה האחרונה ששוחקה, נשמרת ב-progress)
- [ ] מחולל תוכן: הגרל מספר
- [ ] מסך משחק לפי המכניקה (בחירה מרובה (4 אפשרויות))
- [ ] מחולל מסיחים: תשובות שגויות קרובות-מספיק כדי לאתגר
- [ ] ערבוב מיקום התשובה הנכונה
- [ ] חיווי: `IAKidsFX.correct/wrong` + מטבעות בכל מענה
- [ ] בחירת מספר סיבובים במסך הפתיחה (5/10/15, ברירת מחדל 10) — נשמר ב-progress
- [ ] טיימר 💣 לשאלה: \`game.timer({style:'bomb', onTimeout})\` + \`timer.start(IAKidsTimer.secondsFor(diff.level))\`; פקיעת זמן = טעות
- [ ] HUD: ניקוד + מונה שאלות (שאלה X מתוך הסיבובים שנבחרו)
- [ ] שאלות ללא חזרות: כל שאלה דרך \`game.newQuestion(gen, keyFn)\` (השחקן לא מקבל שאלה שכבר ענה עליה)
- [ ] קושי מסתגל: \`game.difficulty(level)\` — תשובה נכונה מקשה, טעות מקלה
- [ ] מסך סיום: כוכבים (1–3 לפי דיוק), ניקוד, **שם שחקן + תאריך ושעה**, שיא אישי, מטבעות, "שחק שוב"
- [ ] כפתור 📤 אתגר לחבר: `game.shareButton(score, containerEl)` במסך הסיום
- [ ] תמיכת שפות: מחרוזות UI דרך `IAKidsLang.t()/ui()` (he/en/es/de/pt); תוכן תלוי-שפה נשאר בשפתו
- [ ] SDK: `saveScore` בסיום (שם+תאריך אוטומטית), `getHighScores` לשיא, `complete(score)` (טורניר/אתגר/מטבעות — אוטומטי)
- [ ] הוסף `'tens-units'` ל-`READY` ב-`games/index.html`
- [ ] בדיקת דפדפן: משחק מלא ללא שגיאות קונסול + צילום מסך

## הגדרת "גמור"

משחק שלם ב-`/games/tens-units/index.html`: פתיחה → בחירת רמה → 10 שאלות → סיום עם קונפטי; מטבעות מתעדכנים בארנק המשותף; שיא נשמר וחוזר אחרי רענון; אפס שגיאות JS.

## רעיונות להמשך (לא לבנות עכשיו)

- צלילי הקראה (TTS עברית) לגילאים צעירים
- אתגר יומי עם פרס מטבעות כפול
- שיתוף תוצאה להורה (parent-dashboard)

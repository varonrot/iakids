# 🏆 מדרג הישגים

| | |
|---|---|
| **מס׳** | 100/100 |
| **Slug** | `leaderboard` |
| **קטגוריה** | ⏱️ שיעורי בית ומוטיבציה |
| **מכניקה** | כלי / מעקב (ללא תשובות נכונות-שגויות) |
| **DB** | `iakids_game_leaderboard` (IndexedDB, דרך ה-SDK) |

## תיאור

טבלת שיאים אישית לשמירת התוצאות הטובות ביותר.

## לוגיקת תוכן

קורא high scores מכל DBs של המשחקים (indexedDB.databases() + פתיחת iakids_game_*); טבלה ממוינת; סה"כ מטבעות; מדליות זהב/כסף/ארד לפי התקדמות.

## רמות

פילטר לפי קטגוריה · "המשחק החזק שלי" · סיכום שבועי


## 🪙 מטבעות

- כלי, אין תשובות שגויות: **+10 🪙** על כל משימה/סשן שהושלם, **+25 🪙** בונוס יומי.
- אין הורדת מטבעות בכלי.

## משימות בנייה

- [ ] העתק שלד מ-`games/demo/index.html` (RTL, game-style.css, game-sdk.js, שלושת המסכים)
- [ ] `IAKidsCoins.mount()` בטעינה — תג המטבעות מוצג
- [ ] עזרה ❓: `IAKidsHelp.mount({slug, how, example})` — הסבר קצר איך משחקים + דוגמה קונקרטית; נפתח אוטומטית בביקור ראשון
- [ ] מצבים/הגדרות (ראה "רמות" למעלה) נשמרים ב-saveProgress
- [ ] מחולל תוכן: קורא high scores מכל DBs של המשחקים (indexedDB.databases() + פתיחת iakids_game_*)
- [ ] מסך משחק לפי המכניקה (כלי / מעקב (ללא תשובות נכונות-שגויות))
- [ ] שמירה מתמשכת ב-DB של המשחק (saveProgress) — הנתונים הם המוצר
- [ ] איפוס/מחזור יומי לפי תאריך (new Date().toDateString())
- [ ] חיווי: `IAKidsFX.correct/wrong` + מטבעות בכל מענה (השלמת משימה)
- [ ] HUD: ניקוד + מונה שאלות/מצב
- [ ] מסך סיום: כוכבים (1–3 לפי דיוק), ניקוד, **שם שחקן + תאריך ושעה**, שיא אישי, מטבעות, "שחק שוב"
- [ ] כפתור 📤 אתגר לחבר: `game.shareButton(score, containerEl)` במסך הסיום
- [ ] תמיכת שפות: מחרוזות UI דרך `IAKidsLang.t()/ui()` (he/en/es/de/pt); תוכן תלוי-שפה נשאר בשפתו
- [ ] SDK: `saveScore` בסיום (שם+תאריך אוטומטית), `getHighScores` לשיא, `complete(score)` (טורניר/אתגר/מטבעות — אוטומטי)
- [ ] הוסף `'leaderboard'` ל-`READY` ב-`games/index.html`
- [ ] בדיקת דפדפן: משחק מלא ללא שגיאות קונסול + צילום מסך

## הגדרת "גמור"

משחק שלם ב-`/games/leaderboard/index.html`: פתיחה → שימוש בכלי ושמירת נתונים → סיום עם קונפטי; מטבעות מתעדכנים בארנק המשותף; שיא נשמר וחוזר אחרי רענון; אפס שגיאות JS.

## רעיונות להמשך (לא לבנות עכשיו)

- צלילי הקראה (TTS עברית) לגילאים צעירים
- אתגר יומי עם פרס מטבעות כפול
- שיתוף תוצאה להורה (parent-dashboard)
